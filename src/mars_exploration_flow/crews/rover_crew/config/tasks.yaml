plan_rover_operations:
  description: >
    You MUST call the tool `rover_navigation_plan`.

    The tool returns a JSON object matching RoverPlanOutput.

    CRITICAL RULES:
    - DO NOT transform the tool output
    - DO NOT summarize
    - DO NOT add text
    - DO NOT wrap the output
    - Your FINAL ANSWER MUST BE exactly the JSON returned by the tool
    - If you do not call the tool, the answer is INVALID

    Inputs:
    - rovers: {rovers}
    - mission_report: {mission_analysis_file}

    Return ONLY the tool result.
  expected_output: >
    A JSON object matching RoverPlanOutput exactly.
  agent: rover_planner
